# Local Claude Code settings (machine-specific paths)
.claude/settings.local.json

# Source material and personal data — kept local, not published
transcripts/
rdforex/

# here.now publish-state cache (local machine state, not site content)
.herenow/

# macOS
.DS_Store
